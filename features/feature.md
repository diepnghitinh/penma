# Features 

9. Export to Json
+ Figma: 
- Must have export image and svg if have.
- 

9. Import web
+ Modal Import from url, add checkbox allow Automatically generate design system components.
design system components will be contained in a new frame.
8. Component
- Component references are independent of the original component's position.
- In Component references, right click support go to main component.
7. Keyboard Shortcuts
- Support copy/paste element: copy element & children element, paster too
6. 
- A component can be copied to multiple elements, and when the original component is changed, the copies (refs) also change accordingly.
- Component references cannot be edited.
- Supports uncomponent conversion, changing a component element to a normal element.
5. 
- Layer elements with the type "component" always display in pink.
- These elements are components that will display a pink border when hovered over.
- Also pink in sidebar
4. In case, import image from web import to Penma project.
3. Allows you to create multiple projects.
1 project includes: name, pages, elements. ....
2. Support mongodb to store all status of project: pages, elements, ... ensure that 
1. Disable right click native, When right click to element, show menu: Create component, Group selection, move to page, Bring to front, Send to back